package ru.hrhappiness.dao;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;
import org.springframework.stereotype.Service;
import ru.hrhappiness.controller.TypeProjectRestController;



import java.util.Optional;
/*


@Service
public class TypeProjectDao {

    TypeProjectRepository typeProjectRepository;
    @Autowired
    public TypeProjectDao(TypeProjectRepository typeProjectRepository) {
        this.typeProjectRepository = typeProjectRepository;
    }

    public Optional<TypeProject> findByIdTypeProject(Integer id){
        return typeProjectRepository.findById(id);
    }
}
*/
